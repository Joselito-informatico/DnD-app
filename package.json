{
  "name": "dnd-ecosystem",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "player": "npm run dev --workspace=apps/player",
    "build:player": "cd apps/player && npm run build"
  }
}
{
  "name": "dnd-ecosystem",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "player": "npm run dev --workspace=apps/player",
    "build:player": "npm run build --workspace=apps/player"
  }
}